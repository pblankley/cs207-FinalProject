language: python
python:
    - "3.5"
before_install:
    - pip install pytest pytest-cov
    - pip install coveralls
    - pip install --use-mirrors matplotlib
env:
    - TEST_DIR=code
script:
    - cd $TEST_DIR && py.test
after_success:
    - coveralls
