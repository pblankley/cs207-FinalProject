language: python
python:
    - "3.5"
install:
    - sudo apt-get install python3-matplotlib
before_install:
    - pip install pytest pytest-cov
    - pip install coveralls
env:
    - TEST_DIR=code 
script:
    - cd $TEST_DIR && py.test
after_success:
    - coveralls
